# Production Environment Variables
ENVIRONMENT=production
DATABASE_URL=postgresql://username:password@host:port/database
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend
NODE_ENV=production
VITE_API_BASE_URL=https://your-backend-url.onrender.com

# Security
CORS_ORIGINS=https://chamchisangsa.github.io,http://localhost:3000
RATE_LIMIT_PER_MINUTE=60

# Database Options:
# 1. Render PostgreSQL (무료): DATABASE_URL will be automatically set
# 2. Fly.io PostgreSQL (무료): DATABASE_URL will be automatically set
# 3. Railway PostgreSQL (유료): DATABASE_URL will be automatically set
